---
name: Scheduled Workflow
on:
  workflow_dispatch:
  schedule:
    - cron: 0 0 * * *
jobs:

  scheduled-job:

    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "This workflow runs every day at midnight UTC"